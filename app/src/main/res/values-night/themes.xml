<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StopWatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="CustomBottomBarStyle" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="itemIconTint">@color/red</item>
        <item name="itemTextColor">@color/red</item>
    </style>

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/transparent</item>
        <item name="searchHintIcon">@drawable/search</item>
        <item name="searchIcon">@drawable/search</item>
        <item name="android:textColor">@color/tint</item>
        <item name="android:textColorHint">#FAD6DE</item>
    </style>
</resources>